import React from "react";

const Home = () => {
  return (
    <div>
      <div>homepage</div>
      <button onClick={() => console.log("click me")}>click me</button>
    </div>
  );
};

export default Home;
